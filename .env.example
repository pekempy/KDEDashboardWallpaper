# Turns on GET /api/public/* (see README "Public API"). Leave unset/blank to
# keep the whole namespace 404ing - that's the default, safe state.
# Generate one with: openssl rand -hex 32
PUBLIC_API_TOKEN=
